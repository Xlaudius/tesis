<%= form_with(model: property_street_location, local: true) do |form| %>
  <% if property_street_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(property_street_location.errors.count, "error") %> prohibited this property_street_location from being saved:</h2>

      <ul>
      <% property_street_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :property_id %>
    <%= form.text_field :property_id %>
  </div>

  <div class="field">
    <%= form.label :street_location_id %>
    <%= form.text_field :street_location_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
