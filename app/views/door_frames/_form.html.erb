<%= form_with(model: door_frame, local: true) do |form| %>
  <% if door_frame.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(door_frame.errors.count, "error") %> prohibited this door_frame from being saved:</h2>

      <ul>
      <% door_frame.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
