<%= form_with(model: property_pavement, local: true) do |form| %>
  <% if property_pavement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(property_pavement.errors.count, "error") %> prohibited this property_pavement from being saved:</h2>

      <ul>
      <% property_pavement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :pavement_id %>
    <%= form.text_field :pavement_id %>
  </div>

  <div class="field">
    <%= form.label :property_id %>
    <%= form.text_field :property_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
