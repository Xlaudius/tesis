<%= form_with(model: dry_wall, local: true) do |form| %>
  <% if dry_wall.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dry_wall.errors.count, "error") %> prohibited this dry_wall from being saved:</h2>

      <ul>
      <% dry_wall.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
