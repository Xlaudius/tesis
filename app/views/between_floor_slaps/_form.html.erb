<%= form_with(model: between_floor_slap, local: true) do |form| %>
  <% if between_floor_slap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(between_floor_slap.errors.count, "error") %> prohibited this between_floor_slap from being saved:</h2>

      <ul>
      <% between_floor_slap.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
