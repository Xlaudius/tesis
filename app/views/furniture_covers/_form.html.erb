<%= form_with(model: furniture_cover, local: true) do |form| %>
  <% if furniture_cover.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(furniture_cover.errors.count, "error") %> prohibited this furniture_cover from being saved:</h2>

      <ul>
      <% furniture_cover.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
