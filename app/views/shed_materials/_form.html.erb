<%= form_with(model: shed_material, local: true) do |form| %>
  <% if shed_material.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shed_material.errors.count, "error") %> prohibited this shed_material from being saved:</h2>

      <ul>
      <% shed_material.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
