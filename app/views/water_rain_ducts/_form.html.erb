<%= form_with(model: water_rain_duct, local: true) do |form| %>
  <% if water_rain_duct.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(water_rain_duct.errors.count, "error") %> prohibited this water_rain_duct from being saved:</h2>

      <ul>
      <% water_rain_duct.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
