<%= form_with(model: property_roofing, local: true) do |form| %>
  <% if property_roofing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(property_roofing.errors.count, "error") %> prohibited this property_roofing from being saved:</h2>

      <ul>
      <% property_roofing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :property_id %>
    <%= form.text_field :property_id %>
  </div>

  <div class="field">
    <%= form.label :roofing_id %>
    <%= form.text_field :roofing_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
