<%= form_with(model: property_cover, local: true) do |form| %>
  <% if property_cover.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(property_cover.errors.count, "error") %> prohibited this property_cover from being saved:</h2>

      <ul>
      <% property_cover.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :observations %>
    <%= form.text_area :observations %>
  </div>

  <div class="field">
    <%= form.label :property_id %>
    <%= form.text_field :property_id %>
  </div>

  <div class="field">
    <%= form.label :cover_id %>
    <%= form.text_field :cover_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
