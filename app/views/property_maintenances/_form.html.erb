<%= form_with(model: property_maintenance, local: true) do |form| %>
  <% if property_maintenance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(property_maintenance.errors.count, "error") %> prohibited this property_maintenance from being saved:</h2>

      <ul>
      <% property_maintenance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :maintenance_id %>
    <%= form.text_field :maintenance_id %>
  </div>

  <div class="field">
    <%= form.label :property_id %>
    <%= form.text_field :property_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
