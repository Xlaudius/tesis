<%= form_with(model: exterior_closure, local: true) do |form| %>
  <% if exterior_closure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exterior_closure.errors.count, "error") %> prohibited this exterior_closure from being saved:</h2>

      <ul>
      <% exterior_closure.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
