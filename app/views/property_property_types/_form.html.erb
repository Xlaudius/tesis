<%= form_with(model: property_property_type, local: true) do |form| %>
  <% if property_property_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(property_property_type.errors.count, "error") %> prohibited this property_property_type from being saved:</h2>

      <ul>
      <% property_property_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :property_type_id %>
    <%= form.text_field :property_type_id %>
  </div>

  <div class="field">
    <%= form.label :property_id %>
    <%= form.text_field :property_id %>
  </div>

  <div class="field">
    <%= form.label :sites %>
    <%= form.number_field :sites %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
