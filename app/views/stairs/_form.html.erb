<%= form_with(model: stair, local: true) do |form| %>
  <% if stair.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stair.errors.count, "error") %> prohibited this stair from being saved:</h2>

      <ul>
      <% stair.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
