<%= form_with(model: property_stair, local: true) do |form| %>
  <% if property_stair.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(property_stair.errors.count, "error") %> prohibited this property_stair from being saved:</h2>

      <ul>
      <% property_stair.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :property_id %>
    <%= form.text_field :property_id %>
  </div>

  <div class="field">
    <%= form.label :stair_id %>
    <%= form.text_field :stair_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
