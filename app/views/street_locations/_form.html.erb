<%= form_with(model: street_location, local: true) do |form| %>
  <% if street_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(street_location.errors.count, "error") %> prohibited this street_location from being saved:</h2>

      <ul>
      <% street_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
