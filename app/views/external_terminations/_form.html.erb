<%= form_with(model: external_termination, local: true) do |form| %>
  <% if external_termination.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(external_termination.errors.count, "error") %> prohibited this external_termination from being saved:</h2>

      <ul>
      <% external_termination.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
