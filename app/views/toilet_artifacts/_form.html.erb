<%= form_with(model: toilet_artifact, local: true) do |form| %>
  <% if toilet_artifact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(toilet_artifact.errors.count, "error") %> prohibited this toilet_artifact from being saved:</h2>

      <ul>
      <% toilet_artifact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
