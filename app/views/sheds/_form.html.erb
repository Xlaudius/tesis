<%= form_with(model: shed, local: true) do |form| %>
  <% if shed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shed.errors.count, "error") %> prohibited this shed from being saved:</h2>

      <ul>
      <% shed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
