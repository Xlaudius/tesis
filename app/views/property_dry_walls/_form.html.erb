<%= form_with(model: property_dry_wall, local: true) do |form| %>
  <% if property_dry_wall.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(property_dry_wall.errors.count, "error") %> prohibited this property_dry_wall from being saved:</h2>

      <ul>
      <% property_dry_wall.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :dry_wall_id %>
    <%= form.text_field :dry_wall_id %>
  </div>

  <div class="field">
    <%= form.label :property_id %>
    <%= form.text_field :property_id %>
  </div>

  <div class="field">
    <%= form.label :other %>
    <%= form.text_field :other %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
