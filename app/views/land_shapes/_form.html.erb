<%= form_with(model: land_shape, local: true) do |form| %>
  <% if land_shape.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(land_shape.errors.count, "error") %> prohibited this land_shape from being saved:</h2>

      <ul>
      <% land_shape.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
